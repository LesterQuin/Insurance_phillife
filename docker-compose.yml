services:
  app:
    build: .
    container_name: insurance-system-app
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
    volumes:
      - ./uploads:/app/uploads
      - ./temp_uploads:/app/temp_uploads
    restart: unless-stopped
